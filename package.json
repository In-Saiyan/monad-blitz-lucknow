{
  "name": "bp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "clean": "rm -rf .next .turbo out",
    "clean:all": "rm -rf .next .turbo out node_modules package-lock.json",
    "db:generate": "npx prisma generate",
    "db:push": "npx prisma db push",
    "db:reset": "npx prisma migrate reset --force",
    "setup": "npm install && npx prisma generate && npx prisma db push",
    "setup:clean": "npm run clean:all && npm install && npx prisma generate && npx prisma db push",
    "setup:db": "npm run clean:all && npm install && npx prisma generate && npx prisma migrate reset --force",
    "compile": "hardhat compile",
    "node:start": "hardhat node",
    "node:background": "hardhat node &",
    "deploy:localhost": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:monad": "hardhat run scripts/deploy.ts --network monadTestnet",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "test:contracts": "hardhat test",
    "test:deployed": "hardhat run scripts/test-contracts.ts --network localhost"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@heroicons/react": "^2.2.0",
    "@next-auth/prisma-adapter": "^1.0.7",
    "@prisma/client": "^6.13.0",
    "@types/bcryptjs": "^2.4.6",
    "@types/multer": "^2.0.0",
    "@types/ws": "^8.18.1",
    "bcryptjs": "^3.0.2",
    "ethers": "^6.15.0",
    "multer": "^2.0.2",
    "next": "15.4.5",
    "next-auth": "^4.24.11",
    "prisma": "^6.13.0",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@openzeppelin/contracts": "^5.4.0",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.4.5",
    "hardhat": "^2.26.1",
    "tailwindcss": "^4",
    "tsx": "^4.20.3",
    "typescript": "^5"
  }
}
